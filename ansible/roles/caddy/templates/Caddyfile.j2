# Caddy Configuration - Auto-generated by Ansible
# TLS termination for CIRISBridge services

{
    email {{ caddy_email }}
    acme_ca https://acme-v02.api.letsencrypt.org/directory

    # Trust Cloudflare proxy IPs for real client IP forwarding
    # Source: https://www.cloudflare.com/ips/
    servers {
        trusted_proxies static 173.245.48.0/20 103.21.244.0/22 103.22.200.0/22 103.31.4.0/22 141.101.64.0/18 108.162.192.0/18 190.93.240.0/20 188.114.96.0/20 197.234.240.0/22 198.41.128.0/17 162.158.0.0/15 104.16.0.0/13 104.24.0.0/14 172.64.0.0/13 131.0.72.0/22 2400:cb00::/32 2606:4700::/32 2803:f800::/32 2405:b500::/32 2405:8100::/32 2a06:98c0::/29 2c0f:f248::/32
    }
}

# Billing API
billing1.{{ dns_soa }} {
    reverse_proxy ciris-billing:8000 {
        health_uri /health
        health_interval 30s
    }

    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        X-Content-Type-Options nosniff
        X-Frame-Options DENY
        -Server
    }

    log {
        output file /var/log/caddy/billing.log
        format json
    }
}

# LLM Proxy
proxy1.{{ dns_soa }} {
{% if proxy_enabled | default(true) %}
    reverse_proxy ciris-proxy:{{ proxy_port }}
{% else %}
    # Proxy disabled via proxy_enabled=false
    respond "Service temporarily unavailable" 503
{% endif %}

    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        X-Content-Type-Options nosniff
        X-Frame-Options DENY
        -Server
    }

    log {
        output file /var/log/caddy/proxy.log
        format json
    }
}

# Agents endpoint (same as billing for now)
agents.{{ dns_soa }} {
    reverse_proxy ciris-billing:8000 {
        health_uri /health
        health_interval 30s
    }

    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        X-Content-Type-Options nosniff
        X-Frame-Options DENY
        -Server
    }
}

{% if inventory_hostname == 'vultr' %}
# CIRISLens - Observability (US node only)
lens.{{ dns_soa }} {
    # CIRISLens API routes (log ingestion, admin) - specific paths only
    handle /ingest/* {
        reverse_proxy cirislens-api:8000
    }
    handle /lens-api/* {
        uri strip_prefix /lens-api
        reverse_proxy cirislens-api:8000
    }

    # Everything else goes to Grafana (including /api for Grafana's own API)
    handle {
        reverse_proxy cirislens-grafana:3000
    }

    header {
        Strict-Transport-Security "max-age=31536000; includeSubDomains; preload"
        X-Content-Type-Options nosniff
        -Server
    }

    log {
        output file /var/log/caddy/lens.log
        format json
    }
}
{% endif %}

# Root domain - redirect to docs or landing page
{{ dns_soa }} {
    respond "CIRIS Infrastructure - {{ dns_soa }}" 200
}
